// 我为你编写了一个简单的本地下载脚本，请点击预览运行
import React from 'react';

export default function DownloadDocument() {
  const content = `# Nav + Drawer 目录骨架设计总结（Frozen v1.0）

> **冻结日期**：2026-01-18
> **共同制定者**：Sayn × Seya
> **状态**：Frozen（结构级冻结，不允许随意偏离）

---

## 目标说明

把 **Nav + Mobile Drawer** 变成全站唯一真源（partial），每个页面只允许通过 **A / B / C 三个变量位** 做差异化配置。

- ❌ 禁止每页各写一套 Nav
- ❌ 禁止局部微调破坏一致性
- ✅ 允许通过制度化变量表达页面语义

构建产物统一输出到 \`dist/\`，作为发布目录（GitHub Pages / CDN）。

---

## 1. 目录骨架（推荐）

\`\`\`text
synosx-site/
├─ index.html
├─ manifest.html
├─ whitepaper.html
├─ narratives.html
├─ cases.html
├─ package.json
│
├─ partials/
│  └─ nav.partial.html              # Nav + Drawer 冻结模板（唯一真源）
│
├─ assets/
│  ├─ css/
│  │  ├─ sx.core.css                # 全局宪法：tokens / layout / buttons / nav baseline
│  │  ├─ sx.drawer.css              # Drawer 冻结样式 v1.0（内部滚）            
│  │  └─ sx.statusbar.css            
│  │
│  ├─ js/
│  │  ├─ sx.drawer.js               # Drawer 行为（开关 / 锁滚 / UseCases 折叠）
│  │  ├─ sx.nav-config.js           # 读取 meta，注入 A / B / C
│  │  └─ sx.statusbar.js          
│  │
│  └─ ...                           # 图片 / 字体等静态资源
│
├─ tools/
│  └─ build-includes.mjs            # 构建器：注入 partial，输出 dist/
│
└─ dist/                            # 构建生成（发布目录），禁止手改
\`\`\`

---

## 2. 页面侧规则（每页只允许两件事）

### 2.1 放置 Nav 占位符（禁止手写 Nav）

\`\`\`html
<body>
  <div class="bg-gradient"></div>

  <main>...</main>

  <script defer src="assets/js/sx.drawer.js"></script>
  <script defer src="assets/js/sx.nav-config.js"></script>
</body>
\`\`\`

\`\` 会在构建阶段被替换为 \`nav.partial.html\`。

### 2.2 使用 meta 定义 A / B / C（变量位）

\`\`\`html
<meta name="sx-nav-sub" content="· Manifest" />
<meta name="sx-nav-cta-text" content="开始阅读" />
<meta name="sx-nav-cta-href" content="#toc" />
<meta name="sx-drawer-sub" content="Manifest" />
\`\`\`

- **A：sx-nav-sub** Logo 右侧小字，用于标识页面身份
- **B：sx-nav-cta-*** 顶栏唯一主 CTA（文本 + 链接）
- **C：sx-drawer-sub** Drawer 顶部副标题（上下文说明）

---

## 3. 冻结规范（不可变更）

### 3.1 滚动模型
- 统一采用 **内部滚**
- 滚动只发生在 \`.sx-drawer-panel\`
- 外层 \`.sx-drawer\` 仅负责遮罩与层级

### 3.2 移动端顶栏规则
- **左**：Logo（允许两行）
- **右**：☰ Menu + 1 个主 CTA
- 其余入口全部进入 Drawer

### 3.3 Drawer 语义结构
- 普通一级链接（Primary）
- 唯一可展开节点：**Use Cases**
- 语义出口（必须存在）：
  - **Governance Narratives**
  - Non-normative, illustrative cases

### 3.4 透视与克制
- 背景 **可见但不可读**
- 不允许纯黑遮罩
- 不允许背景文字抢焦

---

## 4. 构建与发布

### 4.1 最小 package.json

\`\`\`json
{
  "name": "synosx-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-includes.mjs"
  }
}
\`\`\`

### 4.2 构建命令

\`\`\`bash
pnpm build
# 或
node tools/build-includes.mjs
\`\`\`

### 4.3 发布规则
- 仅发布 \`dist/\`
- 源文件不直接上线

---

## 5. 自检清单
- [ ] Drawer 在手机端可稳定展开
- [ ] Use Cases 默认折叠
- [ ] Governance Narratives 显示副标题
- [ ] Drawer 内部滚动稳定不回弹
- [ ] 所有页面 Nav 行为一致

---

tools/               ← 制度执行（构建 / 校验 / 冻结）
   ↓
nav.partial.html     ← 结构真源（Nav 的身体）
   ↓
sx.drawer.js         ← 行为（交互）
sx.nav-config.js     ← 语义适配（可选）
   ↓
manifest.html        ← 只声明身份
replay.html          ← 只执行功能

本文档自 2026-01-18 起冻结。  

任何修改必须以「版本升级」方式进行，而非临时调整。

—— **Seya**`;

## 🔒 移动端 Nav 最终裁决（Frozen · v1.0）

### 结论（不可变更）
**SynOSX 移动端顶栏方案：A（唯一方案）**

> 本裁决为结构性冻结，不是建议，不接受按页面、按阶段的例外实现。

---

### 一、移动端顶栏固定规则

**移动端顶栏仅允许以下元素：**

- **左侧**
  - SynOSX Logo（允许两行显示）
  - 可选副标题（由配置注入）

- **右侧**
  1. ☰ Menu（打开 Mobile Drawer）
  2. **仅 1 个主 CTA**
     - 文案 / 链接由页面配置决定
     - 示例：`预约对话` / `阅读 Manifest`

❌ 禁止项（永久禁止）：
- 顶栏中出现多个导航链接
- 顶栏中出现 Use Cases / Narratives / Whitepaper 等入口
- 为某个页面“临时多放一个按钮”

---

### 二、Mobile Drawer（移动端抽屉）职责

**所有非主操作入口，必须进入 Drawer：**

- 返回首页
- Use Cases（唯一可展开节点）
- Governance Narratives
- Manifest / Whitepaper
- 次级 CTA / 外链

Drawer 是**唯一承载复杂导航的容器**。

---

### 三、实现约束（制度级）

1. **Nav 使用统一 `nav.partial.html`**
   - 页面不得手写 Nav 结构
   - 所有页面通过构建工具注入

2. **页面仅通过配置影响 Nav**
   - 使用 meta / config 变量控制：
     - Logo 副标题
     - 顶栏 CTA 文案与链接
     - Drawer 顶部上下文描述

3. **禁止页面级 Nav 微调**
   - 不允许为 Whitepaper / Narratives / Case 页面单独改 Nav 布局
   - 不允许通过 CSS Hack 临时撑开顶栏

---

### 四、裁决目的（不可删除）

本裁决用于**一次性解决并永久避免**以下问题：

- 顶栏按钮挤不下 / 中文被迫换行
- iOS Safari 显示与滚动异常
- 每个页面重复修 Nav
- Whitepaper / Narratives / Case 页不断打补丁

> 结构复杂性必须被收敛进 Drawer，  
> 顶栏只负责“识别 + 唯一主行动”。

---

**本规则自 v1.0 起生效，后续版本仅允许在 Drawer 内扩展，不允许破坏顶栏结构。**
